import { NextRequest, NextResponse } from "next/server";
const apiUrl = process.env.NEXORA_API_URL ?? process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:4000/api";
async function forward(request: NextRequest, context: { params: { path: string[] } }) { const key = process.env.ADMIN_API_KEY, password = process.env.ADMIN_DASHBOARD_PASSWORD; if (!key || !password) return NextResponse.json({ error: { message: "The administrator environment is not configured." } }, { status: 500 }); if (request.headers.get("x-admin-dashboard-password") !== password) return NextResponse.json({ error: { message: "Administrator password is incorrect." } }, { status: 401 }); const response = await fetch(`${apiUrl}/admin/${context.params.path.join("/")}`, { method: request.method, headers: { "Content-Type": "application/json", "x-admin-api-key": key }, body: ["GET", "DELETE"].includes(request.method) ? undefined : await request.text(), cache: "no-store" }); const payload = response.status === 204 ? null : await response.json().catch(() => ({ error: { message: "The quiz API returned an invalid response." } })); return new NextResponse(payload ? JSON.stringify(payload) : null, { status: response.status, headers: payload ? { "Content-Type": "application/json" } : undefined }); }
export async function GET(request: NextRequest, context: { params: { path: string[] } }) { return forward(request, context); }
export async function PATCH(request: NextRequest, context: { params: { path: string[] } }) { return forward(request, context); }
export async function DELETE(request: NextRequest, context: { params: { path: string[] } }) { return forward(request, context); }
