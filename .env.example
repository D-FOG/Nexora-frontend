NEXT_PUBLIC_API_URL=http://localhost:4000/api
# Server-only: must match the backend ADMIN_API_KEY. Do not prefix with NEXT_PUBLIC_.
ADMIN_API_KEY=replace-with-a-long-random-admin-api-key
# Server-only password required to submit imports from /admin.
ADMIN_DASHBOARD_PASSWORD=choose-a-separate-strong-admin-password
